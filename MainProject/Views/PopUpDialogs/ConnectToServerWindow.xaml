<Window x:Class="Library_System_Management.Views.PopUpDialogs.ConnectToServerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Connect" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Select Server Type" FontWeight="Bold"/>
            <ComboBox Name="CmbServer" Margin="0,5" ItemsSource="{Binding types}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=ServerTypeName}" />
                    </DataTemplate> 
                    </ComboBox.ItemTemplate>
                </ComboBox>
            <Label Content="ServerAdresss"/>
            <TextBox Name="TxtServerAdresss" Margin="0,5"/>
            <Label Content="" Name="LabelConnection" Margin="50,50,0 ,0"/>
            <Button Content="Connect..." Click="BtnConnect_Click"/>
            <Button Content="Save" Click="BtnSave_Click"/>
            
        </StackPanel>
    </Grid>
</Window>
