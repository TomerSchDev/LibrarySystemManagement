<Window x:Class="Library_System_Management.Views.BookInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Library_System_Management.Views"
        mc:Ignorable="d"
        Title="BookInfoWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <GroupBox Header="Book Information" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding SelectedBook.Title}" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedBook.Author}" />
                <TextBlock Text="{Binding SelectedBook.ISBN}" />
                <TextBlock Text="{Binding SelectedBook.Quantity}" />
                <TextBlock Text="{Binding SelectedBook.Available}" />
            </StackPanel>
        </GroupBox>
        <Button Grid.Row="1" Content="Export Book Info" Width="*" Height="20" Margin="0,-100,0,0" Click="BtnExport_Click"/>
    
        <!-- Current Borrowed Books -->
        <GroupBox Grid.Row="2" Header="Current Borrowed Copies" Padding="10" Margin="0,0,0,10">
            <DockPanel>
                <!-- Borrowed Books Table -->
                <DataGrid ItemsSource="{Binding CurrentBorrows}" AutoGenerateColumns="False"
                          CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Member ID" Binding="{Binding MemberID}" Width="*"/>
                        <DataGridTextColumn Header="Member Name" Binding="{Binding Member.FullName}" Width="*"/>
                        <DataGridTextColumn Header="Borrow Date" Binding="{Binding BorrowDate}" Width="150"/>
                        <DataGridTextColumn Header="Expected Return" Binding="{Binding ExpectedReturnDate}" Width="150"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Open Member Info" Margin="2"
                                                Command="{Binding DataContext.MemberInfoCommend, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <!-- Borrow History -->
        <GroupBox Grid.Row="3" Header="Borrow History" Padding="10">
            <DataGrid ItemsSource="{Binding BorrowHistory}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Member ID" Binding="{Binding MemberID}" Width="*"/>
                    <DataGridTextColumn Header="Member Name" Binding="{Binding Member.FullName}" Width="*"/>
                    <DataGridTextColumn Header="Borrow Date" Binding="{Binding BorrowDate}" Width="150"/>
                    <DataGridTextColumn Header="Expected Return" Binding="{Binding ExpectedReturnDate}" Width="150"/>
                    <DataGridTextColumn Header="Returned On" Binding="{Binding ReturnDate}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <Button Grid.Row="4" Content="Export Book Borrow History" Width="*" Height="20" Margin="0,-100,0,0" Click="BtnExportHistory_Click"/>
    </Grid>
</Window>
