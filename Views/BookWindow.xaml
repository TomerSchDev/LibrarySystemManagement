<Window x:Class="Library_System_Management.Views.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Books" Height="400" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Name="dgBooks" AutoGenerateColumns="False" Grid.Row="0" Margin="0,0,0,10" IsReadOnly="True"
                  MouseDoubleClick="dgBooks_MouseDoubleClick"
                  >
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookId}" Width="50"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Available" Binding="{Binding Available}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add Book" Width="100" Margin="5" Click="BtnAdd_Click"/>
            <Button Content="Open Book Info" Width="100" Margin="5" Click="BtnOpen_Click"/>
            <Button Content="Edit Book" Width="100" Margin="5" Click="BtnEdit_Click"/>
            <Button Content="Delete Book" Width="100" Margin="5" Click="BtnDelete_Click"/>
        </StackPanel>
    </Grid>
</Window>